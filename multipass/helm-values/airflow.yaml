config:
  api:
    workers: 1
dags:
  storageClassName: apps-hostpath-provisioner
  mountPath: /airflow/dags/
webserver:
  enabled: true
web:
  enabled: true
workers:
   waitForMigrations:
    enabled: true
   persistence:
    size: 10G
scheduler:
  waitForMigrations:
    enabled: true
webserver:
  waitForMigrations:
    enabled: true
apiServer:
  waitForMigrations:
    enabled: true
triggerer:
  waitForMigrations:
    enabled: true
  persistence:
    size: 10G
dagProcessor:
  waitForMigrations:
    enabled: true
migrateDatabaseJob:
  enabled: true
logs:
  persistence:
    # Enable persistent volume for storing logs
    enabled: false
    # Volume size for logs
    size: 10G
triggerer:
  persistence:
    # Enable persistent volume for storing logs
    enabled: true
    # Volume size for logs
    size: 10G
redis:
  persistence:
    # Enable persistent volume for storing logs
    enabled: true
    # Volume size for logs
    size: 5G
dags:
  persistence:
    # Enable persistent volume for storing logs
    enabled: true
    # Volume size for logs
    size: 1G
workers:
  persistence:
    # Enable persistent volume for storing logs
    enabled: true
    # Volume size for logs
    size: 10G
postgresql:
  primary:
    resources:
      requests:
        memory: 128Mi
dags:
  persistence:
    storageClassName: applications-hostpath-csi